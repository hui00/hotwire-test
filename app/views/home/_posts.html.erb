  <div id="<%= dom_id(post) %>" class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
    <h2 class="font-bold text-2xl"><%= post.author %></h2>
    <p class="text-gray-700 text-base"><%= post.content %></p>
  </div>
